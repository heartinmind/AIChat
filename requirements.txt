# 🔧 뷰티 클리닉 AI 챗봇 - Python 의존성 목록

# 기본 설정 관리
pydantic-settings>=2.9.1
pydantic>=2.7.0
python-dotenv>=0.21.0

# 데이터 검증
jsonschema>=4.23.0

# 테스트 프레임워크
pytest>=8.3.5

# 웹 프레임워크 (향후 API 서버용)
# fastapi>=0.104.0
# uvicorn>=0.24.0

# 데이터베이스 (향후 실제 DB 연동용)
# sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.0

# 외부 API 연동 (향후 실제 서비스 연동용)
# requests>=2.31.0
# httpx>=0.25.0

# 로깅 및 모니터링
# structlog>=23.2.0

# Google Cloud SDK (선택사항 - 실제 배포 시)
# google-cloud-aiplatform>=1.36.0
# google-adk>=0.5.0 